apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tsm-helm.fullname" . }}
  labels:
    {{- include "tsm-helm.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- .Values.config.configFile | nindent 4 }}